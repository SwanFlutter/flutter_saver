name: flutter_saver
description: "A package to save image and files to downloads folder all platform."
version: 0.0.1
homepage: "https://github.com/SwanFlutter/flutter_saver"

environment:
  sdk: '>=2.19.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  
  
  flutter:
    sdk: flutter
  http: ^1.2.2
  path_provider: ^2.1.3
  permission_handler: ^11.3.1
  android_path_provider: ^0.3.0
  external_path_ios_mac:
      git:
        url: https://github.com/SwanFlutter/external_path_ios_mac.git
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0


flutter:


